const dogs = ['james', 'peet', 'chance'];

// just another way to get into an array of data!
for(let dog of dogs) {
  console.log(dog);
}

const numbers = [1, 2, 3, 4];
let total = 0;
for(let number of numbers) {
  total += number;
}

console.log(total);

